version: '3.9'

services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1812
      POSTGRES_DB: notes
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - notesapi-network
  web:
    build: .
    restart: on-failure
    command: > 
      sh -c 
      "python manage.py makemigrations &&
      python manage.py migrate &&
      gunicorn settings.wsgi:application --bind 0.0.0.0:8000"
    volumes:
      - .:/django-app
    depends_on:
      - db
    environment:
      DB_NAME: notes
      DB_USER: postgres
      DB_PASSWORD: 1812
      DB_HOST: db
      DB_PORT: 5432
    networks:
      - notesapi-network
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - ./staticfiles:/django-app/staticfiles:ro
    depends_on:
      - web
    networks:
      - notesapi-network

networks:
  notesapi-network:
    driver: bridge
volumes:
  postgres_data: